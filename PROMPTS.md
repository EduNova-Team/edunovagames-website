In all UI development prompts, address the prompt in a context similar to the below with navigating to the browser via MCP and observing the UI. 

"Navigate to localhost:3000, generate a basic component, review the styling, and update the generation prompt in PROMPTS.md to produce better components going forward." 